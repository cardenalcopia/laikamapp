import { ActividadesUsuario } from './actividades-usuario';

describe('ActividadesUsuario', () => {
  it('should create an instance', () => {
    expect(new ActividadesUsuario()).toBeTruthy();
  });
});
